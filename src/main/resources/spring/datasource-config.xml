<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:jdbc="http://www.springframework.org/schema/jdbc"
       xmlns:jee="http://www.springframework.org/schema/jee"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
         http://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/context
         http://www.springframework.org/schema/context/spring-context.xsd
         http://www.springframework.org/schema/jdbc
         http://www.springframework.org/schema/jdbc/spring-jdbc.xsd
         http://www.springframework.org/schema/jee
         http://www.springframework.org/schema/jee/spring-jee.xsd">

    <!-- Configurer that replaces ${...} placeholders -->
    <context:property-placeholder location="classpath:spring/data-access.properties" system-properties-mode="OVERRIDE"/>

    <!-- ✅ Tomcat JDBC Connection Pool with HA optimization -->
    <bean id="dataSource" class="org.apache.tomcat.jdbc.pool.DataSource" destroy-method="close">
        <property name="driverClassName" value="${jdbc.driverClassName}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
        <property name="initialSize" value="${jdbc.initialSize:5}"/>
        <property name="maxActive" value="${jdbc.maxActive:20}"/>
        <property name="maxIdle" value="${jdbc.maxIdle:5}"/>
        <property name="minIdle" value="${jdbc.minIdle:5}"/>
        <property name="maxWait" value="${jdbc.maxWait:10000}"/>
        <property name="testOnBorrow" value="${jdbc.testOnBorrow:true}"/>
        <property name="testOnReturn" value="${jdbc.testOnReturn:true}"/>
        <property name="testWhileIdle" value="${jdbc.testWhileIdle:true}"/>
        <property name="timeBetweenEvictionRunsMillis" value="${jdbc.timeBetweenEvictionRunsMillis:10000}"/>
        <property name="minEvictableIdleTimeMillis" value="${jdbc.minEvictableIdleTimeMillis:30000}"/>
        <property name="maxAge" value="${jdbc.maxAge:300000}"/>
        <property name="validationQuery" value="${jdbc.validationQuery:SELECT 1}"/>
        <property name="validationInterval" value="${jdbc.validationInterval:5000}"/>
        <property name="removeAbandoned" value="${jdbc.removeAbandoned:true}"/>
        <property name="removeAbandonedTimeout" value="${jdbc.removeAbandonedTimeout:60}"/>
        <property name="logAbandoned" value="${jdbc.logAbandoned:true}"/>
    </bean>

    <!-- Database initializer -->
    <jdbc:initialize-database data-source="dataSource">
        <jdbc:script location="${jdbc.initLocation}"/>
        <jdbc:script location="${jdbc.dataLocation}"/>
    </jdbc:initialize-database>

    <beans profile="javaee">
        <!-- JNDI DataSource for JEE environments -->
        <jee:jndi-lookup id="dataSource" jndi-name="java:comp/env/jdbc/petclinic"/>
    </beans>
</beans>
